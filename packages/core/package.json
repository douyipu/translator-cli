{
  "name": "@douyipu/translator-cli-core",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build ./index.ts --outdir=dist --target=node",
    "dev": "bun --hot ./index.ts",
    "test": "bun test",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.18",
    "ai": "^5.0.48",
    "zod": "^4.1.11"
  }
}
